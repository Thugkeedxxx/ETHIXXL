client.on('messageCreate', (message) => {if (message.author.bot) return;

  // React to every message with 🇨🇦 emoji
  message.react('🔥'🥺'🥶'❤'🤭'😅'🇨🇦').catch(console.error);
});